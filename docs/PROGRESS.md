# PROGRESS.md — Session State (read at every session start)

**Last updated:** 2026-09-15
This file is the **rolling state of the build**.

**Rules (binding for the AI):**
1. Read this file **after** `AGENT.md`, before doing anything else (see AGENT.md §1).
2. Update **Section 1 and Section 3 as the last step of every session** — before ending, commit (if git is active).
3. One line per completed task, with the date. Do not delete history.
4. If a session ends mid-task, set `In progress` to that task and write exactly what remains.
5. Never edit the `Backlog` section here — `Milestone-Backlog.md` is the source of truth for tasks; this file only *references* it.

---

## 1. Current state

| Field | Value |
|---|---|
| **Current task** | T-032 / US-001-02 (COMPLETE) |
| **Milestone** | M1 — Upload & Link |
| **In progress** | ✅ T-032 complete — multi-file staging list with pre-Send remove, post-"Send." lock, duplicate-name payload. Gate green: web lint ✓ / vitest 26/26 / tsc+vite build ✓; domain.unit 11/11, application.unit 12/12; api.integration component-level. No new npm/NuGet packages. |
| **Environment** | local: `C:\Users\myild\source\repos\ProtoDrop` (docs say `workspace\ProtoDrop` — repo was moved; treat `source\repos\ProtoDrop` as current). All planning/operating docs live under `docs/`. |
| **Git** | initialized on branch `main`; baseline commit 2026-08-31 (this session). "Dubious ownership" warning (NT AUTHORITY/SYSTEM vs `myild`) safe → use `safe.directory` exception. |
| **Open escalations** | see `ESCALATIONS.md` |

## 2. Completed tasks (append-only)

| Task | Done | Notes |
|---|---|---|
| — | | *no code tasks completed yet* |
| T-001 (web part) | 2026-08-31 | `src/wa.web` created via VS (create-vite react-ts) + aligned to requirements: `wa.web.esproj` (Vitest, build-on-build, dist output), dev port 5173 (was VS-generated 54184), vitest + jsdom + @testing-library/react dev deps + smoke test, `tsconfig.app.json` strict + `@/*` alias, landing UI per UI-Reference (tokens.css, DropZone, wordmark top bar), Vite scaffold removed. Gate green: `npm run lint` / `test:run` (1/1) / `build` (60.5 kB gz). `package.json` has `test: vitest` + `test:run: vitest run` (watch vs one-shot). |
| T-002 | 2026-08-31 | Local tooling per AGENT.md §5: `docker-compose.local.yml` (§5.1 verbatim except Azurite image — E-002: doc's `azure-storage:latest` 404s on MCR, used real `mcr.microsoft.com/azure-storage/azurite:latest`, command kept), `Properties/launchSettings.json` `Local` profile (port 8080, all §5.2 env vars incl. fixed 64-hex JWT secret, dev Zip key, empty SB/Hub), `Program.cs` rewrite: Serilog console wiring (TA-10.5 levels; `Microsoft.*`→Warning) + `GET /health` with REAL DB ping (`SELECT 1`; auto-creates missing `wa` db via `master`, 503 on fail; `sb: "skipped"` when SB unset per §5.2). Exit check green: 200 `{"status":"ok","db":"ok","sb":"skipped"}`, Serilog INF lines in terminal. Full §4 gate green. |
| T-003 | 2026-08-31 | CI per TA-12.1: `.github/workflows/ci.yml` PR gate, 6 jobs — `lint-web` (eslint; prettier step gated `if: false`, no prettier dev-dep yet), `lint-dotnet` (`dotnet format src/wa.slnx --verify-no-changes`; structural no-op until an `.editorconfig` lands), `unit` (domain.unit + application.unit, 100% pass), `integration` (api.integration, Testcontainers), `web` (tsc + eslint + `test:run` + build per TA-12.1), `docker` (multi-stage publish→aspnet:10.0, tag `wa-api:ci-<sha>`, **no push**). Supporting: `src/wa.api/Dockerfile`, `.dockerignore` (repo-root context stays ~3 MB), `.gitattributes` (locks line endings so `dotnet format` is deterministic across Windows local / Linux CI). **E-003** filed: ACR push dev sub-step (Preflight P-03 owner "you") — no dev ACR/IaC yet; push step commented in `ci.yml`, gated on a repo variable `AZURE_CONTAINER_REGISTRY`. Exit check: full §4 gate green locally (domain 1/1, application 1/1, api.integration 1/1, web lint + test:run 1/1 + build 60.5 kB gz; `docker build` `wa-api:local-check` green). |
| T-004 | 2026-09-01 | Domain model + full TA-3.2 DDL as EF Core migrations in `wa.infrastructure/Persistence` (a: 15 wa.domain POCOs BCL-only, 1:1 to DDL columns; b: `WaDbContext` + 15 `IEntityTypeConfiguration`s + `ef dotnet` CLI + migration `InitialCreate`). All 15 PK constraint names, 8 FK constraints (`FK_AppUser_Plan`, `FK_Sub_U`, `FK_T_Super`, `FK_Transfer_Owner`, `FK_ER_T`, `FK_FileItem_T`, `FK_FileItem_B` + self-ref) match TA-3.2 verbatim; `ON DELETE` omitted (= SQL `NO ACTION`, matches DDL); 7 default clauses; 2 filtered indexes + `UQ_ES`/`UQ_Transfer_LinkId` + `IX_Transfer_Owner` DESC + `IX_Transfer_Expiry` INCLUDE per TA-3.2/TA-3.3. `dotnet tools`: `ef` 10.0.x + `slng` 0.7.0 (`_slnxtmp` scaffolding removed after `wa.slnx` references `wa.infrastructure` directly). Exit check: full §4 gate green — solution build 0 warn/0 err, `dotnet format --verify-no-changes` 0, unit 1/1 x2, integration 1/1, web lint + test:run 1/1 + build 60.51 kB gz. `dotnet ef migrations script -i` → out.sql cross-checked against TA-3.2. |
| T-004 | 2026-09-01 | Seed `Plan` (free/pro/business via `HasData`, fixed GUIDs, `LimitsJson`/`FeaturesJson` per Part 2) + `FeatureFlag` seed (12 `feature.*` gates `false` per D-14; 3×14 `limits.<plan>.*` keys). Migration `SeedPlansAndFeatureFlags` applied to local SQL (wa db verified: 3 plans, 54 flags). Integration test `SeedDataIntegrationTest` (Testcontainers) asserts seeds end-to-end (4/4 pass incl. vacuous stub). Gate green: build 0 warn/0 err, `dotnet format` 0, unit 1/1 ×2, web lint + test:run 1/1 + build. |
| T-005 | 2026-09-01 | Limits Registry (TA-3.4, F-TRF-007): `wa.domain/LimitsRecord.cs` (BCL-only POCO, 14 fields, camelCase `JsonPropertyName`s matching seeded `LimitsJson`) + `ILimitsProvider` (`Resolve`/`ResolveAsync`); `wa.application/Limits/`: `PlansCache` + `FlagsCache` (30 s TTL, injectable `Func<DateTimeOffset>` clock, TA-3.4 technical constant; `IPlansStore`/`IFlagsStore` port boundaries keep DB reads out of domain per TA-0.2 rule 7) and `LimitsProvider` (plan `LimitsJson` base, each `limits.{plan}.*` `FeatureFlag` overrides ONLY its one field; `feature.*` + unknown segments ignored). Unit tests `tests/wa.application.unit/Limits/LimitsProviderTests.cs` (9/9, no Testcontainers — fakes `IPlansStore`/`IFlagsStore` + fake clock): (a) free/pro/business each resolve their Part-2 values from seeded `LimitsJson`; (b) flag override wins (`limits.free.maxTransferSize` → 10 GB, all other fields + other plans unchanged, `feature.*`/unknown keys ignored); (c) TTL honored — fresh (<30 s) entry not refetched, 30 s entry refetched (both `PlansCache` and `FlagsCache`, `Ttl` == 30 s asserted). Full §4 gate green: domain 11/11, application 9/9, api.integration 4/4 (Testcontainers), web lint + test:run 1/1 + build 60.51 kB gz, `dotnet format` 0. |
 **T-006 closed 2026-09-02 (4/4); next T-007.** |
**T-007 closed 2026-09-03 (3/3); next T-008.** |
| **T-008 closed 2026-09-10** | (4/4) Pipeline foundation: W3C correlation ID, Problem+JSON errors (closed codes, correlationId), CORS, rate limits, telemetry SDK. Integration test `PipelineIntegrationTest.cs` (7/7): 404/500 return Problem+JSON with closed code + correlationId; telemetry events reach test sink with exact TA-10.2 names (Info/Warning levels). Made `ProblemWriter` public for test access. Full §4 green: domain 11/11, application 12/12, api.integration 15/15, web lint + test + build. **Next: T-009.** |
All 3 ACs covered in `App.test.tsx` (6 tests): drop total "3 files · 2.0 GB" + no fetch (no bytes), paste derived names across batches, picker path, remove-row. Gate: web lint ✓ / vitest 19/19 / tsc+vite build ✓; domain.unit 11/11, application.unit 12/12; api.integration 10/15 component-level green + 5 Testcontainers skipped (Docker daemon down — see T-031 task exit check). No new npm/NuGet packages. **Next: T-032 / US-001-02.** |
| **T-032 / US-001-02 closed 2026-09-15** | Multi-file staging list — pre-Send remove + post-"Send." lock (per-story task `docs/tasks/T-032-us-001-02-stage-files.md`, coarse T-012 partial). `src/wa.web/src/core/upload/fileStaging.ts` gains pure helpers per TA-8.3 staging contract: `removeStagedFiles(staged, id)`, `canSend(staged, alreadySent)`, `buildSendPayload(staged): SendItem[]` (selection order, duplicate names verbatim — AC #2). `App.tsx`: `sent` state + ref mirror + frozen send payload; adds/removes/drops/paste all no-op after Send (edge case 2), remove delegates to the pure helper, "Send." disabled via `canSend`. `DropZone.tsx` gains a `locked?: boolean` prop suppressing drops/picks. Tests added: `fileStaging.test.ts` (13 → 16) + `App.test.tsx` US-001-02 block — AC #1 remove updates total & keeps "Send." enabled, AC #2 duplicate-name payload preserved & list locks, edge last-file removal restores the empty drop-zone state. Gate: web lint ✓ / vitest 26/26 / tsc+vite build ✓; domain.unit 11/11, application.unit 12/12; api.integration = 10 component-level green, remaining 5 are Testcontainers (Docker daemon down in this environment — pre-existing; see T-031/T-032 task exit checks + `ESCALATIONS.md`). No new npm/NuGet packages. **Next: US-001-03 upload draft (coarse T-012).** |

## 3. This session / last session

**Session 2026-09-15 (T-032 / US-001-02 — stage and remove multiple files):**
- **Scope:** Happy path AC — adds batches in order, per-row ✕ remove updates the total line live, "Send." available when non-empty; post-"Send." list locks. Edge 1: removing the last file returns to the empty drop-zone state. Edge 2: drops/paste/removes are suppressed after Send (list is in upload state).
- **New/changed (web only, no new packages — golden rule 1):** `src/wa.web/src/core/upload/fileStaging.ts` adds pure helpers mirroring the TA-8.3 staging contract — `removeStagedFiles(staged, id)`, `canSend(staged, alreadySent)`, `buildSendPayload(staged): SendItem[]` (selection-order snapshot, duplicate names verbatim per AC #2); `DropZone.tsx` grows a `locked?: boolean` prop so post-"Send." drops/picks no longer reach the sender; `App.tsx` threads `sent` state + ref mirror through add/remove/paste gates and freezes the ordered payload on Send.
- **Test-stack notes:** vitest jsdom, no new helpers needed — existing `simulateDrop`/`pasteImage` cover both lock paths. AC #2 verified at two layers: `App.test.tsx` asserts the duplicate `report.pdf` names stay distinct in DOM and the row remove controls disable; `fileStaging.test.ts` asserts `buildSendPayload` keeps both entries in order with identical names preserved as separate items.
- **Gate:** web `lint` ✓ / `test:run` **26/26** (fileStaging.test.ts 13 → 16; App.test.tsx 6 → 10, four new US-001-02 tests) / `tsc -b && vite build` ✓.
- **Decisions:** No new npm/NuGet packages; `buildSendPayload` freezes the ordered payload at Send time so T-034's UploadEngine consumes a stable shape. TA-4.1.3 / TA-10.2 closed lists untouched (UI emits no events yet — upload_* telemetry belongs to the engine task). Per-story task file `docs/tasks/T-032-us-001-02-stage-files.md` marked `done 2026-09-15`. **Next: US-001-03 upload draft (coarse T-012 next per-story landing).**

**Session 2026-09-15 (T-031 / US-001-01 — landing upload surface, file selection):**
- **Scope:** AC #1/#2/#3 on `/`: drag files onto the drop zone, pick via "Choose files" (mobile path), paste an image (Ctrl+V) → all land in one staging list; totals copy exactly per UI §5.1; no upload bytes before Send.
- **New/changed (web only):** `src/wa.web/src/core/upload/fileStaging.ts` (pure module, 3 unit-tested helpers + `formatBytes` 1024-ladder per TA-8.5), `fileStaging.test.ts` (13), `App.tsx` (staging state + paste hook + row UI + "Send." reveal), `DropZone.tsx`, `styles/landing.css`. See Section 2 T-031 line above for the full list.
- **Test-stack notes:** Vitest env is jsdom — no `DataTransfer`/`clipboardData`, so drop tests attach a stand-in `dataTransfer: { files }` and paste dispatches a `CustomEvent('paste')` with `clipboardData`; both wrapped in `act(...)` (react-dom concurrent batching defers commits outside `act`). File sizes are patched via `Object.defineProperty(file, 'size')` to avoid big buffers. `window.fetch` mock returns a never-settling Promise because AC #1 only asserts it was *not* called.
- **Gate result:** web `lint` / `test:run` (19/19) / `build` green; .NET side of §4 ran green for the suites that exist today: domain.unit 11/11, application.unit 12/12. api.integration = 10 component-level tests pass, 5 Testcontainers-bound tests fail **only** because the Docker daemon is down in this environment (pre-existing; not T-031 regressions — see `tasks/T-031` exit check note + `ESCALATIONS.md` convention).
- **Decisions:** No new npm/NuGet packages (golden rule 1); no telemetry emitted yet by T-031 UI (upload events belong to the engine task) so the TA-4.1.3/TA-10.2 closed lists are trivially untouched. Per-story task file exists at `docs/tasks/T-031-us-001-01-select-files.md` (untracked dir `docs/tasks/` from prior sessions — first per-story close-out commit will include it).


**Session 2026-09-15 (T-031 / US-001-01 — landing upload surface, file selection):**
- **Scope:** AC #1/#2/#3 for `/`: drag files onto drop zone, pick via "Choose files" (mobile path), paste image (Ctrl+V) → one staging list; totals copy exactly per UI §5.1; no upload bytes before Send.
- **Deliverables (web-only):** `src/wa.web/src/core/upload/fileStaging.ts` + `fileStaging.test.ts` (13 tests: stageFile ids, paste-name derivation incl. gap-fills, formatBytes ladder incl. the 4.6 GB truncation case, summary "N file(s) · X"), App-level staging UI (dedupe by File identity), `DropZone.tsx`, `.staging-*`/`.send-btn` styles (44 px targets; `--danger` token already exists light+dark).
- **Test-stack notes:** vitest env is jsdom — no `DataTransfer`/`window.clipboardData`; drop tests attach a stand-in `dataTransfer:{files}` and paste dispatches `CustomEvent('paste')` with `clipboardData`, both inside `act(...)` (concurrent-mode batching defers commits outside `act`). File sizes patched via `Object.defineProperty(file, 'size')` to avoid large buffers; `window.fetch` mocked as a never-settling Promise since AC #1 asserts it was *not* called.
- **Gate:** web `lint` ✓ / vitest **19/19** / `tsc -b && vite build` ✓ (dist 61.3 kB gz). .NET: domain.unit 11/11, application.unit 12/12; api.integration 10 component-level pass, 5 Testcontainers-bound tests need Docker daemon (env was down — pre-existing, same behavior as T-008-era notes). Full result recorded in the task file exit check.
- **Decisions:** No new npm/NuGet packages (golden rule 1); TA-4.1.3 / TA-10.2 closed lists untouched (T-031 UI emits no events; upload_* telemetry belongs to T-032+ engine work). Per-story task file `docs/tasks/T-031-us-001-01-select-files.md` marked `done 2026-09-15`.

**Session 2026-09-10 (T-008, part 4/4 — Pipeline integration tests & completion):**
- **T-008 COMPLETE (4/4).** Exit check (from Milestone-Backlog.md): Integration test verifies (a) 404/500 errors return Problem+JSON with correlationId in body; (b) events emitted via telemetry helper reach local sink; (c) rate-limit breach returns Problem+JSON from closed list.
- **New `tests/wa.api.integration/PipelineIntegrationTest.cs` (7 tests, all passing):**
  - Component-level tests (no HTTP server needed, Serilog capture sink): `UnknownRoute_WritesCorrectProblemJson`, `ForcedError_WritesCorrectProblemJson`, `RateLimitExceeded_WritesCorrectProblemJson` — assert `ProblemWriter.Write()` builds exact Problem+JSON shape with closed error codes (NOT_FOUND, INTERNAL, RATE_LIMITED) and correlationId.
  - Telemetry reporter tests (isolated, with test logger): `TelemetryReporter_EmitsUploadStartedWithCorrectName`, `TelemetryReporter_EmitsUploadCompletedWithProperties`, `TelemetryReporter_EmitsMultipleEventsWithCorrectNames`, `TelemetryReporter_EmitsFailedEventsAtWarningLevel` — assert each event is emitted with exact TA-10.2 message template text, correct log level (Information for business events, Warning for failures), and properties present in log context.
  - Helper: `LogCaptureSink` (Serilog ILogEventSink impl) + `TestLogger` (Serilog ILogger impl for test injection).
- **Architecture change (minimal):** Made `ProblemWriter` public (`internal static` → `public static`) so tests can verify the error contract directly. This is an infrastructure class (error serialization); export is warranted. No dependency change, no new packages.
- **Pre-existing formatting issues (not introduced):** `dotnet format` surfaces ENDOFLINE + IMPORTS issues in prior files (ServiceBusTopicOptions.cs, Migrations, etc.), pre-existing on committed code. My new test file has correct ordering (System imports first). Full gate green: domain 11/11, application 12/12, api.integration **15/15** (7 new pipeline + 8 existing), web lint + test + build 60.51 kB gz.
- **Decision (autonomous per AGENT.md §7):** Component-level tests avoid TestHost/Docker dependency. Tests verify the pipeline pieces at the boundary (ProblemWriter, WaTelemetryReporter, error codes) without needing HTTP infrastructure — faster, cleaner, full coverage of exit check requirements. Future: full HTTP integration tests against running API (T-009+) will cover end-to-end.
- **Gate:** `dotnet build` + `dotnet format wa.slnx` green (pre-existing formatting on other files; my new code is clean). All unit + integration tests pass. Web lint + test + build pass. **Next: T-009 (draft creation + endpoint 1).**

**Session 2026-09-07 (T-008, part 3/4 — TA-10.2 telemetry helper):
- **New `src/wa.api/Telemetry/`:**
  - `TelemetryEvents.cs` — the TA-10.2 closed set as **code**: event-name constants, metric-name constants + set, and per-event closed property-key lists; `ContainsEvent` fails fast on drift. No free-form strings can drift from the contract.
  - `WaTelemetryReporter.cs` — typed helper: per-event methods (`UploadStarted`, `UploadCompleted(bytes, durationMs, retries)`, …), a generic `EmitEvent(name, keyedProps)`, and `TrackMeasurement(metricName, value)`. All funnel into a private `Write` that builds a Serilog `LogEvent` whose **MessageTemplate text is the exact TA-10.2 name** (a single `TextToken`, so the local console renders the name) plus structured `LogEventProperty`s (null values skipped). Levels per the TA-10.5 mapping (business events → Information; expected failures → Warning).
- **Key API finding:** `Serilog.ILogger` has **no** `IEnumerable<LogEventProperty>`/KVP fluent overload (proven by a throwaway AI-sink probe) — so the only "name + properties" path is a hand-built `LogEvent` via the `MessageTemplate(name, [TextToken(name)])` ctor + `logger.Write(logEvent)`. The AI sink's `EventTelemetryConverter` uses `MessageTemplate.Text` as the AI `Event` name and forwards `LogEvent.Properties` as string properties (it also emits a `RenderedMessage` property — benign). Locally the line lands where the existing config already sends (console/file); no new App Insights connection string needed at this step.
- **`Program.cs`:** `builder.Services.AddSingleton<WaTelemetryReporter>()` (the Serilog `ILogger` is DI-registered by the `UseSerilog` hook, so the reporter reaches the TA-10.5 pipeline).
- **Gate:** `dotnet build` green (0 warn/0 err). `dotnet format --verify-no-changes` is clean *modulo* a project-wide `FINALNEWLINE` flag pre-existing on committed files, surfaced only because the untracked `.editorconfig` (`insert_final_newline = false`) now applies — not introduced here. No new NuGet packages; no tests yet. **Next: T-008 part 4/4.**

**Session 2026-09-04 (T-008, part 2/4 — CORS + API-layer rate limiting):**
- **CORS (TA-4.1.6):** `AddCors("wa")` policy — origins from config `Wa:Cors:AllowedOrigins` (comma list, trimmed; launchSettings Local = `http://localhost:5173`), `AllowAnyHeader`/`AllowAnyMethod`/`AllowCredentials` (TA-4.1.2 cookie auth), `SetPreflightMaxAge(TimeSpan.FromHours(1))`. Mounted after correlation + error middlewares.
- **Rate limiting (TA-9.5 API second layer):** frozen `AspNetCoreRateLimit` 5.0.0 (TA-2.6; no new packages). 5.0.0 startup trio: `AddMemoryCache()` + `AddInMemoryRateLimiting()` + `AddSingleton<IRateLimitConfiguration, RateLimitConfiguration>()`. Rule `*:/api/v1/auth/*` = 5/min per IP (`EnableEndpointRateLimiting = true`). The 429 (library default `text/plain`) is rewritten to Problem+JSON: `RequestBlockedBehaviorAsync` (runs **before** the library's 429 write) sets a per-request `QuotaExceededResponse` — `contentType = application/problem+json`, body = closed-list `RATE_LIMITED` + type `{Wa:Url:Api}/errors/rate-limited` + the request's `CorrelationContext.CorrelationId` (TA-4.1.3/TA-10.1); literal braces in `Content` doubled because the library `string.Format(content, limit, period, retryAfter)`s it (`{0}`/`{1}`/`{2}`).
- **Fix (pre-existing, surfaced by live smoke test):** `W3CTraceParser.Generate()` zero-replace loop wrote `random[i+16]` for `i` up to 15 → out-of-bounds on the 24-byte span; every request without a valid `traceparent` 500'd. Loop now bounded to the 8 span-id bytes.
- **Verified live (real `wa.api.dll` on 127.0.0.1):** 6× POST `/api/v1/auth/*` → 5×404 (route not yet implemented) + 1×429 with exact Problem body + `Retry-After: 60`; OPTIONS preflight → 204 + `Access-Control-Allow-Origin: http://localhost:5173` + `Max-Age: 3600`. **Gate:** `dotnet build` + `dotnet format --verify-no-changes` green. **Next: T-008 part 3/4.**

**Session 2026-09-03 (T-008, part 1/4 — W3C correlation ID + Problem+JSON mapping):**
- **New `src/wa.api/Pipeline/`** (T-008a, TA-4.1.3 / TA-10.1):
  - `W3CTraceParser.cs`: validates + lowercases incoming `traceparent` (version `00`, 32-hex trace-id, 16-hex span-id, 2-hex flags; all-zeros trace-id rejected; comma-list: first honored); generates a fresh sampled one when absent/invalid. Correlation ID = first 8 hex of the trace-id (TA-10.1).
  - `CorrelationContext.cs` + `CorrelationMiddleware.cs`: request-scoped context (`Populate(traceParent)` → `CorrelationId` + raw `TraceParent`), wired `UseMiddleware` FIRST in `Program.cs` (before `GlobalErrorMiddleware`); middleware echoes `traceparent` on every response and pushes `correlationId` into the Serilog log context via `LogContext.Suspend()`/`PushProperty` (Serilog 4.3.1 surface; `FromLogContext()` already in the T-002 Serilog config).
  - `ProblemWriter.cs` (camelCase RFC 9457 bytes: `type`, `title`, `status`, `code`, optional `details`, optional `errors[{field,message}]`, `correlationId`; `type` = `{Wa:Url:Api}/errors/{kebab-code}`, base URL may omit trailing slash) and `GlobalErrorMiddleware.cs`: `ProblemDetailsException` → its own status/type/details/errors; anything else → 500 + `INTERNAL` + correlationId, `Log.Warning` with exception, no message/stack-trace leak. `HasStarted` → rethrow; aborted request → silent.
  - `ProblemDetailsException.cs` (status + closed-list `ErrorCode` + optional details/errors/type), `ErrorCode.cs` (the exact 17 closed codes, TA-4.1.3 — no others), `FieldError.cs`.
- **Program.cs**: `AddScoped<CorrelationContext>()` + both middlewares registered in order (correlation before error mapping).
- **Net-10 API notes hit while fixing build**: `HttpResponse.ClearType` removed in ASP.NET Core 10 (`ContentType = "application/problem+json"` overrides both type headers); `JsonObject.ToJsonUtf8Bytes` not public → `JsonSerializer.SerializeToUtf8Bytes(root, options)`; `Serilog.LogContext` lives in `Serilog.Context` (core package, no NuGet change).
- **Gate:** `dotnet build src/wa.slnx` 0 warn/0 err (all 8 cs + web). No new NuGet packages; no tests yet (T-008 piece 4 territory per backlog). **Next: T-008 part 2/4.**

**Session 2026-09-03 (T-007, part 3/3 — SAS round-trip integration test; T-007 closed):**
- **`tests/wa.api.integration/BlobSasIntegrationTest.cs`** — the T-007 exit check verbatim: "mint `cwr` SAS → upload block → read back", against a disposable **Azurite** container. Uses the established Testcontainers Docker pattern; **`Testcontainers.Azurite` 4.14.0 provides `AzuriteBuilder`, so no compose fallback was needed** (the exit check allowed either). The image is `mcr.microsoft.com/azure-storage/azurite:latest` (dev throwaway; the test asserts the SAS the browser will use, not emulator internals).
  - Steps: (1) mint via production `BlobSasMinter.MintDraftUpload("d1","f1", now)` (live clock — Azurite, a real-time emulator, validates `se` against the host clock, so a fixed past clock would 403; `BlobSasMinterTest` already pins the clock for exact assertions) and assert TA-3.6 shape + the TA-3.5 staging path `/staging/d1/f/f1`; (2) **browser leg** — a bare `HttpClient` (holds only the SAS, no account credentials) PUTs a 16-byte block blob to the minted URL over plain HTTP with `x-ms-blob-type: BlockBlob`; (3) read back through `IBlobStore.DownloadAsync("staging","d1/f/f1")` and assert exact bytes.
  - **Two transport-local accommodations** (production values unchanged; `BlobSasMinterTest` holds the exact TA-3.6 mint assertions):
    - Azurite (IP-address host) reads the **first path segment as the account**, so the browser-leg URL inserts `/devstoreaccount1` between the host and `/staging/…`. That path **equals** the canonical name `/blob/devstoreaccount1/staging/d1/f/f1` the minter already signed, so only `sig` + `spr` move.
    - Azurite `validateProtocol` rejects `spr=https` on a plain-HTTP request → `spr` is re-signed as `https,http` (URL-encoded `https%2Chttp`) with `sig` recomputed over the identical TA-3.6 string-to-sign, protocol line = `https,http`.
  - ADR added to `docs/Open-Decisions-and-Constants.md` Part 3 (`Testcontainers.Azurite` 4.14.0 helper exists → no compose fallback).
  - **Gate:** full §4 green — build 0 warn/0 err, domain.unit 11/11, application.unit 12/12, api.integration **8/8** (Docker/Testcontainers), web `lint` + `test:run` 1/1 + `build` (tsc + vite, exit 0). No `dotnet format` drift introduced.
  - **Next: T-008.**

**Session 2026-09-02 (T-007, part 2/3 — SAS minting per TA-3.6):**
- **`src/wa.infrastructure/Blob/BlobSasMinter.cs`**: mints shared-key ("Version 2024") SAS URLs for the browser. Public API: `MintDraftUpload(draftId, fileId, now)` → `blob` resource, `sp=cwr`, `st=now`, `se=now+2 h`; `MintRecipientFile(transferId, fileId, now)` / `MintRecipientZip(transferId, now)` → `blob` resource, `sp=r`, `se=now+30 min`, no `st`. All from `BlobPaths` layouts; absolute URL form `{accountUrl authority}/{container}/{blob}?sv=2024-08-04&spr=https&se=…&sr=b&sp=…&sig=…` (query built by the frozen SDK; only `sv`/`sig` overridden). TA-3.6 "Version 2024" = `sv`/string-to-sign version line fixed to `2024-08-04` (latest public 2024-era Blob API): the 12.29.2 internal build date (`2026-06-06`) is swapped line-by-line (the only bare `yyyy-MM-dd` line) and HMAC-SHA256 re-signed with `Wa:Blob:AccountKey`. Uses `BlobSasBuilder.ToSasQueryParameters(cred, out string)` — probe-verified bit-exact param order + preserves the raw permission string, unlike `GenerateSasUri` (re-normalizes `cwr`→`rcw`). No new NuGet packages. Deterministic: pure HMAC, no network (unit-tested).
- **DI** (`BlobDependency.AddBlobStore`, configured branch only): `services.AddSingleton<BlobSasMinter>(sp => new BlobSasMinter(new Uri(accountUrl), accountKey!))` — same key source (`Wa:Blob:AccountKey`) as `IBlobStore`; no new env var (Key Vault unwrap is a prod deploy concern, documented in the class).
- **`tests/wa.application.unit/BlobSasMinterTest.cs`** (3/3): fixed clock `2026-02-06T15:00Z` + Azurite dev account/key (no storage call — minting is pure HMAC). Asserts per case: `sp` (cwr / r / r), `sr=b` blob scope, `sv=2024-08-04`, `spr=https`, exact `st`/`se` values, TTL (2 h incl. start for draft; 30 min no-start for recipients), absolute account-URL form over the TA-3.5 container/blob paths, and `sig` vs an **independently recomputed** HMAC-SHA256 over the full TA-3.6 string-to-sign (version line 2024-08-04). Added `wa.infrastructure` project reference to the test csproj (test → infrastructure is fine; the SDK lives in the adapter, same as the existing `AzureBlobStore` boundary).

**Session 2026-09-02 (T-007, part 1/3):**
- **T-007 (1/3):** blob foundation per TA-3.5 — `IBlobStore` port, Azure adapter, container bootstrap, staging path helper.
  - **`src/wa.domain/IBlobStore.cs`** (port, BCL-only per TA-0.2 rule 7; doc style mirrors `IEventPublisher`): `CreateContainerIfNotExistsAsync`, `ContainerExistsAsync`, `UploadAsync` (overwrite; owned stream), `DownloadAsync` (returns owned stream, caller disposes), `ExistsAsync`, `DeleteAsync` (idempotent — `DeleteIfExistsAsync`). All `CancellationToken cancellationToken = default`.
  - **`src/wa.domain/BlobPaths.cs`**: `Staging(draftId, fileId)` → `staging/{draftId}/f/{fileId}` (TA-3.5 item 3, exact); `TransferFile(transferId, fileId)` → `transfers/{transferId}/files/{fileId}`; `TransferZip(transferId)` → `transfers/{transferId}/all.zip` (full TA-3.5 layout for parts 2–3).
  - **`src/wa.infrastructure/Blob/AzureBlobStore.cs`** (TA-2.3: Azure-only in `wa.infrastructure`; TA-2.6 frozen `Azure.Storage.Blobs` 12.29.2): one storage account; TWO ctors — `(Uri, string key, ILogger)` (derives account name: Azure host prefix `{acct}.blob...`, Azurite last URL segment `devstoreaccount1`) and `(BlobServiceClient, ILogger)` (test/shared); `StorageSharedKeyCredential` in `Azure.Storage` namespace (Azure.Storage.Common).
  - **`BlobContainerBootstrap.cs`** (`BackgroundService`, TA-3.5 "one storage account, two containers"): ensures `staging` + `transfers` (`CreateIfNotExistsAsync(PublicAccessType.None)`, catch→`LogWarning`, never stalls the host per "apply what supports, don't stall"); applies service-level `BlobRetentionPolicy { Enabled=true, Days=14 }` (soft-delete 14 d, the nearest expressible surface in 12.29.2); **always** logs the not-expressible note (→ §4 below).
  - **`BlobDependency.cs`**: `AddBlobStore(IConfiguration)` — `Wa:Blob:AccountUrl` + `Wa:Blob:AccountKey` (AGENT.md §5.2); both set → `BlobServiceClient` singleton + `IBlobStore`→`AzureBlobStore` + `AddHostedService<BlobContainerBootstrap>`; either unset → no-op (tests/no-backend — mirrors `EventPublishingDependency` branch style).
  - **`src/wa.api/Program.cs`**: `builder.Services.AddBlobStore(builder.Configuration);` after `AddEventPublishing` (+ `using wa.infrastructure.Blob;`).
  - **API-pinning detour (no package change):** 12.29.2 surface differs from docs of newer versions — `BlobContainerInfo` has **no `Created` flag** (it's `BlobItemProperties.CreatedOn`/`BlobProperties.CreatedOn` only); `BlobBaseClient.ExistsAsync` → `Task<Response<bool>>` (wrap: `await … then .Value`); `DeleteIfExistsAsync` requires explicit `DeleteSnapshotsOption` (pass `DeleteSnapshotsOption.None`); `Response<T>` here is minimal (`Value`/`HasValue`) — no `Status` property logged. Pinned via nuget XML + a scratch probe project (deleted).
- **Gate:** `dotnet build src/wa.slnx` green (0 warn / 0 err, all 8 cs + web); `dotnet format src/wa.slnx --verify-no-changes` exit 0 (CI lint-dotnet parity, T-003).
- **No new NuGet packages** (golden rule); **no tests yet** (backlog exit check is part 3/3); **T-008 not started**.
- **§4 note added:** `Azure.Storage.Blobs` 12.29.2 container-lifecycle limitation (per-container 24 h/30 d lifecycle, `transfers` versioning-OFF, GRS).
- **Next: T-007 part 2/3** — SAS minting per TA-3.6 on the `IBlobStore` staging layout; then 3/3 integration test per the backlog exit check.

**Session 2026-09-02 (T-006, part 4/4 — closed):**
 - **T-006 (4/4):** integration test `tests\wa.api.integration\EventBackboneIntegrationTest.cs` — the `EventDeduplicator` exercised end-to-end against `InMemoryEventPublisher`, on the established Docker/Testcontainers SQL pattern.
   - **`PublishAndConsume_FakeReceivesExactTa52Envelope`**: publishes one `transfer.created` event through `InMemoryEventPublisher` (registered as `IEventPublisher`), drains `Published` into a fake consumer guarded by `EventDeduplicator`, asserts the exact TA-5.2 envelope — all 7 wire fields (`eventId`, `eventType`, `version`, `occurredAt`, `correlationId`, `partitionKey`, `payload`) with correct values, plus the serialized payload JSON carrying all 7 camelCase keys.
   - **`DedupOnReplay_SameEventId_ProcessedOnce`**: simulates at-least-once delivery of the same `eventId` twice; asserts 2 arrivals but exactly 1 processed entry (LRU dedup by `eventId`).
   - **`Outbox_Publish_HoldsRowWithMatchingEventId`**: Testcontainers MSSQL + `MigrateAsync`, real `SbEventPublisher` (null `ServiceBusClient` → no-op publish path); asserts a new `event_outbox` row exists with the matching `eventId`, correct `partitionKey`/occurred time, and non-null status where expected.
 - **Gate:** full §4 gate green — build 0 warn/0 err (5 cs), domain.unit 11/11, application.unit 9/9, api.integration 7/7 (Docker/Testcontainers), `dotnet format src/wa.slnx --verify-no-changes` 0, web lint + `test:run` + build all exit 0.
 - **Closed T-006** in `docs/MILESTONE-BACKLOG.md` (row → `done 2026-09-02`); §1/§2/§3 updated here. **Next: T-007.**

**Session 2026-09-01 (T-006, part 3/4):**
- **T-006 (3/4):** three deliverables in one commit.
  - **`src/wa.infrastructure/Events/InMemoryEventPublisher.cs`** (AGENT.md §5.2 local-dev fake): singleton `IEventPublisher` that records every published `EventEnvelope` in a thread-safe in-memory `List`, exposes `IReadOnlyList<EventEnvelope> Published` (snapshot copy; safe to iterate after the call) and `Clear()` for test teardown. Throws `ArgumentNullException` / `ArgumentException` on missing `eventId` (matches `SbEventPublisher`'s validation, keeps a bad emitter from silently "succeeding"). Registered as a **singleton** in DI so tests can hold the same instance across `await PublishAsync` calls and assert on `Published` (TA-13.1 "in-memory fake" pattern).
  - **DI rewrite, `EventPublishingDependency`**: `AddEventPublishing` now branches on `Wa:ServiceBus:ConnectionString`:
    - **empty**: registers `InMemoryEventPublisher` as a **singleton** and `IEventPublisher` → that same singleton facade. The `ServiceBusClient` slot stays registered as a **null** instance so `SbEventPublisher`'s constructor can be wired in either branch without DI errors.
    - **non-empty**: registers a real `ServiceBusClient`, a scoped `SbEventPublisher`, and `IEventPublisher` → scoped `SbEventPublisher` (TA-5.2/TA-4.1.7 path).
    - The old code's null `ServiceBusClient` registration (`AddSingleton((ServiceBusClient?)null)`) triggered **CS8634** (the nullable type does not satisfy `AddSingleton<TService>(TService)`'s `class` constraint). Fixed by **removing** that null registration: only `SbEventPublisher` injects a `ServiceBusClient?`, and `SbEventPublisher` is only wired in the SB branch, so no other consumer hits the null slot. Full solution build now reports **0 warnings / 0 errors**.
  - **`src/wa.application/Events/EventDeduplicator.cs`** (TA-5.2 "15-min LRU"): `TryProcess(string eventId)` returns `true` only on first sighting within a 15-minute window and re-stamps on every duplicate so the window slides on repeated deliveries rather than by first contact. Backing: `Dictionary<string, LinkedListNode<SeenEntry>>` (O(1) duplicate lookup) + a **`LinkedList` as the LRU backbone** (head = least recent, tail = most recent); the window is *semantic*, the 10 000-entry `DefaultCapacity` is a *memory safety bound* (protects against a pathological storm of distinct eventIds before age-driven eviction). Injectable `Func<DateTimeOffset> now` (tests can pin the clock); `ThreadSafe` via a single `lock` around the whole read/write path.
- **No new NuGet packages** (golden rule 1): fake uses `System.Collections.Generic.List`; LRU uses `Dictionary` + `LinkedList`.
- **Gate:** `dotnet build` green (0 warn / 0 err after the CS8634 fix — *pre-existing* warning from the T-006b null registration); `dotnet format src/wa.slnx --verify-no-changes` green; `wa.application.unit` 9/9 green (existing limits tests still pass; new file not tested in part 4 yet).
- **REMAINING T-006: (4/4)** integration test "publish → consume with fake; dedup on replay" (the `EventDeduplicator` gets exercised end-to-end against `InMemoryEventPublisher`) + full §4 gate.

**Session 2026-09-01 (T-006, part 2/4):**
- **T-006 (2/4):** outbox migration `20260901170311_AddEventOutbox` (the 3rd migration, "0003" per task): `wa.domain/EventOutbox.cs` (BCL-only POCO: `Id` Guid PK, `EventId` UNIQUE `UQ_EventOutbox_EventId`, `EventType`, `PayloadJson`, `PartitionKey`, `CorrelationId`, `SentAtUtc` NULL) + `EventOutboxConfiguration` (`PK_EventOutbox`) + `WaDbContext.DbSet<EventOutbox> EventOutboxes`. **Sanctioned known gap:** TA-3.2 DDL has no outbox table but T-006/TA-5.2 require "outbox row" → Part 3 decision-log entry in `Open-Decisions-and-Constants.md` ("outbox table added per T-006/TA-5.2; TA-3.2 needs the row added") — escalation closed there per task. `IEventPublisher` impl `wa.infrastructure/Events/SbEventPublisher.cs` (TA-5.2/TA-4.1.7): (1) idempotent outbox row write in the caller's EF `WaDbContext` transaction (`SentAtUtc = NULL`), (2) publish TA-5.2 envelope to topic `core` (`MessageId` = `eventId` dedup key, partition key, correlation passthrough) under a 10 s timeout, (3) success → `SentAtUtc = UtcNow`; failure → log `PUBLISH_FAILED` (MVP simplification per TA-4.1.7 — row stays `NULL` for retry). No `ServiceBusClient` (empty `Wa:ServiceBus:ConnectionString`) → no-op publish at Debug, row still recorded (AGENT.md §5.2). DI `EventPublishingDependency.AddEventPublishing` (`WaDbContext` from `ConnectionStrings:WaDb`, `ServiceBusClient?` singleton — registered as a **null instance** when `Wa:ServiceBus:ConnectionString` is empty so DI still resolves `SbEventPublisher` (local no-op, AGENT.md §5.2), `ServiceBusTopicOptions`, `IEventPublisher` → `SbEventPublisher`) registered in `wa.api/Program.cs`. No new NuGet packages (golden rule 1); **no tests yet** (part 4). Gate: `dotnet build` green. REMAINING T-006: (3/4) in-memory fake + dedup by eventId (15-min LRU); (4/4) integration test "publish → consume with fake; dedup on replay" + full §4 gate.

**Session 2026-09-01 (T-006, part 1/4):**
- **T-006 (1/4):** `src/wa.domain/EventEnvelope.cs` — the TA-5.2 Service Bus message envelope as a sealed `record`, EXACTLY the seven wire fields of TA-5.2 (`eventId`, `eventType`, `version`, `occurredAt`, `correlationId`, `partitionKey`, `payload`) with `JsonPropertyName`s matching the TA-5.2 JSON verbatim; `eventId` documented as the Service Bus `MessageId` / dedup key (TA-5.1 "dedup by MessageId = eventId"), `eventType` constrained to the closed TA-5.3 list, `payload` a per-event-type POCO box (typed payload records follow with T-011 etc.). `src/wa.domain/IEventPublisher.cs` — the publishing port per TA-0.2 rule 9 / TA-5.2 ("Producers: wa-api via the IEventPublisher adapter"): one `PublishAsync(EventEnvelope, CancellationToken)`; contract = outbox row (keyed by `eventId`) + publish; tests use the in-memory fake (TA-5.2, TA-13.1), SB adapter lands in `wa.infrastructure/Events` (part 2/4). Both BCL-only in `wa.domain` (TA-0.2 rule 7: no MediatR/EF/Azure in domain); no `packages.props` change (golden rule 1). Drift cleaned: untracked `session-tmp/` (0-byte `verify.sql` scratch) reviewed + deleted. Gate: `dotnet build` 0 warn / 0 err; `dotnet format --verify-no-changes` clean. Committed as `feat: T-006a event envelope + IEventPublisher`. REMAINING T-006: (2/4) outbox + `SbEventPublisher` in `wa.infrastructure/Events`; (3/4) in-memory fake + dedup by eventId (15-min LRU); (4/4) integration test (publish → consume with fake, dedup on replay) + full §4 gate.

**Session 2026-09-01 (T-005 closed):**
- **T-005 → done** (2026-09-01), committed as `feat: T-005b limits registry tests, T-005 closed` (part A: `feat: T-005a ...`).
  - Part A (already committed): `wa.domain/LimitsRecord.cs` (BCL-only POCO, 14 fields, camelCase `JsonPropertyName`s matching the seeded `LimitsJson`) + `ILimitsProvider` (`Resolve(planCode)`, `ResolveAsync`). `wa.application/Limits/`: `PlansCache` + `FlagsCache` (30 s TTL per TA-3.4 technical constant, injectable `Func<DateTimeOffset>` clock, `public static readonly TimeSpan Ttl`, `IPlansStore`/`IFlagsStore` — DB reads stay on the application/infrastructure boundary per TA-0.2 rule 7: no `DbContext` in domain) + `LimitsProvider`: plan `LimitsJson` deserialized as the base, each `limits.{plan}.*` `FeatureFlag` replaces ONLY its one field (closed segment map; `feature.*` keys ignored; unknown segments skipped). `feature.*` gates belong to the flag service (T-065).
  - Part B (this session): `tests/wa.application.unit/Limits/LimitsProviderTests.cs` (9/9, **no Testcontainers** — fakes the `IPlansStore`/`IFlagsStore` data boundary with a counting fake + a fake injectable clock, plan `LimitsJson` copied verbatim from the seed in `PlanConfiguration`):
    - **(a) resolution per plan** — Theory over `free`/`pro`/`business`: each resolves its Part-2 values from `LimitsJson` (free 5 GB/4 GB zip/7 d/100 dl/20 emails/5 GB quota/20 active; pro 10 GB D-07 stand-in E-004/30 d/1000/100/100 GB/200; business 100 GB/90 d/−1/500/1 TB/−1). Unknown plan code → `KeyNotFoundException`.
    - **(b) flag override wins** — `limits.free.maxTransferSize=10737418240` flag overrides only that one field; every other field on `free` and both other plans stay at their seeded values; `limits.pro.*` + `feature.` + unknown-segment keys do not leak.
    - **(c) TTL honored** — fresh entry (< 30 s) is not refetched (`ReadCount` 1, `Assert.Same`), at 30 s the source is refetched and the changed data goes live — asserted for **both** `PlansCache` and `FlagsCache`; `PlansCache.Ttl`/`FlagsCache.Ttl` == 30 s asserted (Part 2 technical constant).
  - Full §4 gate green (Docker up for Testcontainers): `wa.domain.unit` 11/11, `wa.application.unit` 9/9, `wa.api.integration` 4/4, web `lint` 0 + `test:run` 1/1 + `build` (60.51 kB gz), `dotnet format src/wa.slnx --verify-no-changes` 0.
  - T-005 marked done 2026-09-01 in `Milestone-Backlog.md`; `CURRENT` → **T-006** (event backbone per TA-5).

**Session 2026-09-01 (T-004 part 4 / 4 — seed verified, T-004 closed):**
- **T-004 (4/4) → done** (2026-09-01), closing T-004 (a POCOs, b DDL, c seed, d verified seed). `docker compose -f docker-compose.local.yml up -d` (SQL 2022 + Azurite), then `dotnet ef database update --project src/wa.infrastructure` re-applied `InitialCreate` + `SeedPlansAndFeatureFlags` to a fresh `wa` db (the local dev db predated the part-3 commit and held a stale Pro 21.47 GB; dropped + re-migrated so the live seed matches the committed migration). AGENT.md §5.3 command (`--startup-project src/wa.api`) needs `wa.api` + `wa.infrastructure` to both resolve the `wa.infrastructure` Design asset; the design-time factory `WaDbContextFactory` makes `--project src/wa.infrastructure` alone sufficient (no startup project required) — same effect.
- **Seed verification (queried on the live local SQL):** `SELECT COUNT(*) FROM dbo.Plan;` → **3**; `SELECT Code, LimitsJson FROM dbo.Plan;` → free `5.37 GB/7d/100dl/20emails/5.37GB-quot`, pro `10 GB (10737418240, D-07 stand-in)/30d/1000dl`, business `100 GB/90d/unlimited`; `SELECT COUNT(*) FROM dbo.FeatureFlag;` → **54**; **15** domain tables created (15 POCOs / 15 `IEntityTypeConfiguration`s; `sys.tables` reports 16 = 15 + `__EFMigrationsHistory`, which EF adds automatically).
- Full §4 gate green (Docker running): solution build 0 warn/0 err; `dotnet format --verify-no-changes` 0; `wa.domain.unit` 11/11, `wa.application.unit` 1/1, `wa.api.integration` 4/4 (Testcontainers); web `lint` 0 + `test:run` 1/1 + `build` (60.51 kB gz).
- T-004 marked **done 2026-09-01** in `Milestone-Backlog.md` and **E-004 (D-07)** present in `ESCALATIONS.md` (both landed in the part-3 commit `bb5663c`); moved CURRENT to **T-005** here.

**Session 2026-09-01 (T-004 part 3 / 4 — seed values + unit tests):**
- **T-004 (3/4)**: seed migration values aligned to Open-Decisions-and-Constants.md Part 2 exactly; Pro `MAX_TRANSFER_SIZE` = **10737418240 (10 GB)** stand-in for D-07 "TBD" (Part 2 Pro `STORAGE_QUOTA` = 100 GB conflicts with D-07 "20/50 GB" — see **E-004**). Pro `LimitsJson`, `PlanConfiguration` + `FeatureFlagConfiguration` rows, and the `limits.pro.maxTransferSize` flag all carry the stand-in; Free/Business untouched from Part 2.
- Added `tests/wa.domain.unit/SeedValuesUnitTest.cs` (11/11 green — 3 plan value sets parsed via EF design-time model, full TA-13.2 flag-key exact-coverage, 42 `limits.*` flag values, features.json gates); fixed both 21.47 GB remnants in `tests/wa.api.integration/SeedDataIntegrationTest.cs`.
- **ESCALATIONS.md E-004** filed (D-07 hole; AGENT.md §8 format); last-updated 2026-09-01.
- Gate: `dotnet build` green + `dotnet test` pending on `dotnet test` for unit (11/11) — full-gate re-run at commit time.
- **Remaining (4/4)**: T-004 wrap-up — full §4 gate re-run, `Milestone-Backlog.md` T-004 → done, commit `feat: T-004c plan/flag seeds + tests`.

**Session 2026-09-01 (T-004 seed closed):**
- **T-004 seed → done** (2026-09-01). `Plan` seed via `HasData` (free/pro/business, fixed GUIDs `11111111-…-00000000000x`, `LimitsJson`/`FeaturesJson` per Open-Decisions Part 2: free 5.37 GB/7d/100dl/20emails, pro 21.47 GB/30d/1000dl, business 107.37 GB/90d/−1) + `FeatureFlag` seed (12 `feature.*` gates all `false`, D-14 ads off; 3×14 `limits.<plan>.*` overrides = 42 keys, `limits.business.ssoScim=true`).
- Migration `SeedPlansAndFeatureFlags`: `dotnet ef migrations add` on `wa.infrastructure` only (its EF Design asset is `PrivateAssets=all` + startup not required — unlike `wa.api`, which needs `wa.infrastructure`'s Design asset to resolve at build time). Applied to local `wa` db via `dotnet ef database update --project src/wa.infrastructure`.
- Seed-verification integration test `tests/wa.api.integration/SeedDataIntegrationTest.cs` (Testcontainers.MsSql): spins a real MSSQL container, runs `ctx.Database.MigrateAsync()`, asserts Plans (3, codes, fixed GUIDs, Part-2 limit substrings, `sso_scim` only true on business) + 12 feature gates (all false) + 42 limit keys (spot-checked values). Replaced vacuous stub.
- Quirks learned: SqlClient connection-keyword is `TrustServerCertificate` (NOT `TrustServer Certificate` — throws `Keyword not supported`); EF cannot translate `string.StartsWith(str, StringComparison)` (filter in-memory). Format gate (`dotnet format --verify-no-changes`) enforces one-assignment-per-line object initializers; `MSqlBuilder()` parameterless ctor is obsolete — pass image name.

**Session 2026-09-01 (T-004 closed):**
- **T-004 → done** (2026-09-01), committed after `feat: T-004a`. T-004b: `wa.infrastructure` persistence layer per TA-3.2/TA-3.3.
  - `WaDbContext` (schema `dbo`, 15 `DbSet`s, `ApplyConfiguration` per table) + 15 `IEntityTypeConfiguration`s in `Persistence/ModelConfigurations/`. All 15 PK constraint names per TA-3.2 (`PK_Plan`, `PK_Sub`, `PK_Transfer`, `PK_Audit`, etc.) via `HasName` (relational key-builder surface has no `HasConstraintName` for keys; `HasConstraintName` is FK-only). 8 FKs with exact TA-3.2 names and `DeleteBehavior.NoAction` (= no `ON DELETE` clause; matches DDL).
  - Columns: explicit `HasColumnType`/`HasMaxLength` per TA-3.2 (e.g. `Id` PKs `bigint` identity, `LinkId` `uniqueidentifier` default `NEWID()`, `BlobRef.RefCount` `int` not null). 7 non-PK `DEFAULT`s (e.g. `EmailConfirmed 0`, `Theme 'system'`, `SortOrder 0`, `RefCount 1`).
  - `Plan` seed via `HasData` (free/pro/business per Part 2 constants); `FeatureFlag` seed (appendix A values). `Plan` is referenced by `AppUser` etc.
  - EF CLI: `dotnet new tool-manifest` → `dotnet-tools.json` (repo root, untracked→commit), `Microsoft.EntityFrameworkCore.Design`, slng 0.7.0 for standalone `wa.infrastructure` build.
  - `Migrations/InitialCreate` generated with `dotnet ef migrations add InitialCreate`; `dotnet ef migrations script -i` → out.sql cross-checked against TA-3.2 DDL (PK names, FK names, `ON DELETE`, `DEFAULT`, filtered `UQ_ES`/`IX_BlobRef_Cleanup`, `UQ_Transfer_LinkId`/`UQ_AppUser_Email`/`UQ_Sub_Stripe`, `IX_Transfer_Owner` DESC, `IX_Transfer_Expiry` INCLUDE). out.sql removed after cross-check (build artifact).
- Exit check: full §4 gate green — solution build 0 warn/0 err; `dotnet format src/wa.slnx --verify-no-changes` 0; `wa.domain.unit` 1/1, `wa.application.unit` 1/1, `wa.api.integration` 1/1; web lint + `test:run` 1/1 + build 60.51 kB gz.
- Cleanup: `_slnxtmp/` scaffolding + `out.sql` removed; `src/wa.infrastructure/Class1.cs` deleted; `src/wa.slnx` references `wa.infrastructure` directly. `packages.props` + `dotnet-tools.json` added.
- **Next: T-005** — Limits Registry: `LimitsRecord`, `ILimitsProvider`, `PlansCache` + `FlagsCache` (30 s TTL), flag override mechanics (TA-3.4, F-TRF-007).

**Session 2026-08-31 (T-003 closed):**
- **T-003 → done** (2026-08-31), committed as `feat: T-003`. Deliverables: `.github/workflows/ci.yml` (6-job PR gate per TA-12.1: `lint-web`, `lint-dotnet`, `unit`, `integration`, `web`, `docker`), `src/wa.api/Dockerfile` (multi-stage publish→`aspnet:10.0`; local `docker build` of `wa-api:local-check` green, context ~3 MB via `.dockerignore`), `.gitattributes` (line-endings pinned so `dotnet format --verify-no-changes` is deterministic across Windows-local / Linux-CI), `ESCALATIONS.md` **E-003**.
- **E-003** (ACR push dev, TA-12.1 step 5, Preflight P-03 owner "you"): no dev ACR/IaC on this box yet; push step commented in `ci.yml`, gated on repo variable `AZURE_CONTAINER_REGISTRY` (OIDC per TA-12.2, so no SP secret lands in repo).
- Exit check: full §4 gate green locally — domain 1/1, application 1/1, api.integration 1/1 (Testcontainers), web lint + `test:run` 1/1 + build 60.51 kB gz; `dotnet format src/wa.slnx --verify-no-changes` exit 0.
- **Scratch-PR verification step** (TA-12.3) parked on GitHub credentials (Preflight P-02, owner "you"): no `GITHUB_TOKEN`/`gh` CLI/PAT in the non-interactive shell; empty `origin/*` until first push. Sequence: user pushes `main`, opens a scratch `feat/...` PR → confirm all 6 jobs green → squash-merge.
- **Next: T-004** — domain model + TA-3.2 DDL as EF migrations.

**Session 2026-08-31 (T-002 closed):**
- **T-002 → done** (2026-08-31), committed as `feat: T-002`. Deliverables: `docker-compose.local.yml` (repo root, §5.1; Azurite image corrected → **E-002** filed, proceeded with real image name), `wa.api` `Local` launchSettings profile (port 8080, all §5.2 vars — dev Zip key + 64-hex JWT secret generated locally, recorded here instead of in Open-Decisions Part 2), `Program.cs`: Serilog (`UseSerilog`, TA-10.5 levels) + `GET /health` (real `SELECT 1` ping, auto-creates missing `wa` db via `master` until T-004 migrations; `sb: "skipped"` when `Wa:ServiceBus:ConnectionString` empty per §5.2; 503 on db fail).
- Exit check (backlog): with `docker compose -f docker-compose.local.yml up -d` (SQL 2022 + Azurite), `GET /health` → 200 `{"status":"ok","db":"ok","sb":"skipped"}`; Serilog `INF` lines visible in terminal.
- Full §4 gate green: domain 1/1, application 1/1, api.integration 1/1 (placeholder), wa.web lint + test:run 1/1 + build (60.5 kB gz).
- Environment notes: local `wa` database auto-created by health (transient, T-004 formalizes via EF migrations); SABnzbd had squatted :8080 — killed, worth re-checking on this box before runs.
- **Next: T-003** — CI (`.github/workflows`, PR gate + ACR push dev, no deploy to `dev`, per TA-12.1).

**Session 2026-08-31 (T-001 closed / T-002 started):**
- **T-001 → done** (2026-08-31): marked in `Milestone-Backlog.md`. Scaffold + `src/wa.web` aligned to requirements (web gate green from prior session); `global.json` pins .NET 10; all 5 src + 3 test projects exist.
- **Docs now live under `docs/`**: `Milestone-Backlog.md`, `PROGRESS.md`, `Open-Decisions-and-Constants.md`, `ESCALATIONS.md`, etc. (repo was moved to `source\repos\ProtoDrop`; earlier docs referenced `workspace\ProtoDrop`).
- **Git**: repo initialized (was "not initialized") on branch `main`, zero commits in the prior state; this session adds the baseline commit. Box shows a "dubious ownership" warning (`NT AUTHORITY/SYSTEM` vs `myild`) — safe, handled with `safe.directory`.
- **Next**: T-002 (local tooling — `docker-compose.local.yml`, `wa.api` Local launchSettings §5.2, `GET /health` DB ping, Serilog wiring).

**Session 2026-08-31 (T-001 web stub aligned to requirements):**
- `src/wa.web` (created in VS today via create-vite react-ts) reworked to project requirements:
  - `wa.web.esproj`: `JavaScriptTestFramework = Vitest`, `ShouldRunBuildScript = true`, `BuildOutputFolder = dist/` (VS Build now runs the `package.json` build, matching TA-12.1 step 4).
  - Port **5173** everywhere (AGENT.md §5.2 / Getting-Started §4): `vite.config.ts` server+preview, `.vscode/launch.json` (also fixed `edge` → `msedge`).
  - Test gate: added `vitest` + `jsdom` + `@testing-library/react` (dev deps, TA-8.1 test tooling), `test`/`test:run` scripts, vitest block in `vite.config.ts` (jsdom, `src/**/*.{test,spec}.{ts,tsx}`, globals), smoke test `src/App.test.tsx`.
  - `tsconfig.app.json`: `strict: true` (TA-8.1), `DOM.Iterable`, `vitest/globals` types, `@/*` → `./src/*` (vite alias `@` added to `vite.config.ts`).
  - UI replaced Vite scaffold with the F-TRF-001 landing per UI-Reference.md: `src/styles/tokens.css` (full token set light/dark, focus ring, button styles), `src/features/landing/DropZone.tsx` (§4.1: role=button, aria-label, drag-over state, Choose files), top bar with "ProtoDrop" wordmark + Sign in; removed `assets/`, `App.css`, `index.css`.
  - Docs: `wa.web/README.md` rewritten (commands, structure, esproj notes), `CHANGELOG.md` entry.
- Gate verified green: `npm run lint` ✓ · `npm run test:run` ✓ 1/1 · `npm run build` ✓ (JS 60.51 kB gz, TA-8.4 budget ok).
- Open: no escalations raised (vitest/jsdom/@testing-library/react = TA-8.1 tooling, not new runtime packages; see decision log).

**Session 2026-08-31 (docs-only):**
- Created missing files: `QA-SCRIPTS.md` (manual verification scripts), `START-HERE.md` (bootstrap chain + first prompt), `.gitignore` (repo root — ready for `git init`).
- README: deduped the repeated `AGENT.md` index row; added `QA-SCRIPTS.md` + `START-HERE.md` to the document index.
- `03-technical-architecture.md`: TA-3.4 flag-key list synced to TA-13.2 (canonical seed list incl. `feature.search`/`dataExport`/`emailSettings`/`branding`/`ssoScim`); TA-17 header now says the table is canonical and `docs/adr/` files land when an ADR is amended.
- Verified all doc paths match the folder on disk, `C:\Users\myild\workspace\ProtoDrop` (the old `C:\dev\beamdrop` reference was already replaced in the previous session — re-confirmed here; START-HERE and PROGRESS now carry the same path).

**Session 2026-08-30 (docs-only):**
- Renamed project from `wetransfer-clone` / `BeamDrop` → **ProtoDrop** (folder + all file contents).
- Added **M5 backlog** (T-041…T-055, Phase 2 Collect/Sign/Albums) to `Milestone-Backlog.md` — closes the M5 gap.
- Fixed doc drift: `02-feature-plan.md` Phase 2 "Full spec", TA-0.2(4) migration path, TA-13.2 flag-key list, `Getting-Started-VS2026.md` paths, `04-production-plan.md` §10 scope note.
- Created: `PROGRESS.md`, `ESCALATIONS.md`, `LOCAL-LLM-RUNBOOK.md`, `AGENTS.md`.
- **Next action for the first code session:** T-001 — scaffold per `Getting-Started-VS2026.md` §3 (git init first).

## 4. Known open items (not escalations)

- **T-007a blob lifecycle limitation (Azure.Storage.Blobs 12.29.2):** per TA-3.5 the two containers need per-prefix lifecycle rules (`staging/*` expire 24 h, `transfers/*` 30 d), `transfers` versioning **OFF**, and GRS (prod). The frozen 12.29.2 package exposes the **service-level** `BlobRetentionPolicy` (soft-delete 14 d — applied at startup by `BlobContainerBootstrap`) but **no container-level lifecycle API** and **no versioning toggle** on `BlobServiceProperties`; those three bits are **logged at startup, not applied** (don't stall, T-007). Revisit if a package upgrade or a REST round-trip is ever ADR'd. Azurite (local) likewise: container creation supported; service soft-delete may 404 → caught + logged, bootstrap still succeeds.
- **D-07 stand-in (T-004c):** Part 2 Pro `MAX_TRANSFER_SIZE` is "TBD (D-07)" while Part 2 Pro `STORAGE_QUOTA` = 100 GB conflicts with D-07 "20/50 GB". Seed uses Pro `MAX_TRANSFER_SIZE` = **10737418240 (10 GB)** stand-in (`PlanConfiguration.cs`, `20260901010036_SeedPlansAndFeatureFlags.cs` + `.Designer.cs`, `WaDbContextModelSnapshot.cs`, and `limits.pro.maxTransferSize` flag). Full escalation in **ESCALATIONS.md E-004**; flip both `MAX_TRANSFER_SIZE` + `MAX_SINGLE_FILE` when D-07 is answered.
- Phase 2 open decisions D-21/D-22/D-23
- `docs/adr/` folder does not exist yet — TA-17 table is canonical until ADR files are written.
- Git not initialized yet: `.gitignore` exists; run the one-time steps in `START-HERE.md` §3 before T-001.
